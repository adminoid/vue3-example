import { MainPageData } from 't@/dev/MainPageData'

describe.skip('debugging', () => {
  it('should create data', function () {
    const instance = new MainPageData(3)
    // console.warn(instance.data)
    // console.warn(instance.data.summary.widgets[0])
    // console.info(instance.data.washers[0].widgets[0])
    // console.log(instance.data.washers[1].widgets[0])
    // console.log(instance.data.washers[2].widgets[0])
  })
})
